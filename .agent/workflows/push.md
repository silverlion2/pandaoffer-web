---
description: Push local changes to the remote repository
---
1. Check git status to ensure changes are staged and committed.
// turbo
2. Run `git push` to upload local repository content to a remote repository.
